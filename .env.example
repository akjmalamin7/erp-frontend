# Base URL of the AZM-ERP Express backend.
# In dev, requests to /api are proxied to this target (see vite.config.ts),
# so you can usually leave this pointing at the same origin.
VITE_API_BASE_URL=/api
